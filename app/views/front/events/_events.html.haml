.events-view.events-view-list
  .row
    .span9
      -cache ['events_whole_list', local_assigns[:cats], local_assigns[:search]], :expires_in => 1.minute do 
        -events.each do |event|
          =Rails.cache.read(event.cache_key + '_event_cache_job')